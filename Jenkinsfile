pipeline {
  agent any
  stages {
    stage("build") {
      steps {
        echo 'build step done'
      }
    }
    stage("run") {
      steps {
        echo "test.js should be run here"
      }
    }
  }
}
